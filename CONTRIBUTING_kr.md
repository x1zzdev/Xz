# Xz에 기여하기

시간을 내줘서 고맙다. 이 프로젝트는 모양이 조금 특이하니, 몇 분만 읽고 시작하면 나중에 거절당할 일을 줄일 수 있다.

## 어떤 프로젝트인가

Xz는 명세로 정의되는 언어다. 이 저장소의 결과물은 `docs/`와 `xz-cli/`의 레퍼런스 구현이다. 가장 값진 기여는 언어를 *더 검토하기 쉽게* 만드는 쪽이다. 그게 이 프로젝트의 존재 이유다. 문서 변경과 파서 변경은 무게가 같고, 때로는 문서 변경이 더 중요하다.

## 기여할 수 있는 일

- `docs/`의 명세를 고치거나 명확하게 만든다.
- `examples/`의 예제를 추가하거나 바로잡는다.
- 컴파일러와 명세가 어긋나는 지점을 알린다.
- `xz-cli/`의 명령을 구현하거나 개선한다.
- 테스트를 보강한다. 명세가 정직하게 유지되는 방법이 테스트다.
- 취약점은 [SECURITY.md](SECURITY.md)의 비공개 절차로 제보한다.

## 시작하기 전에

[docs/01-philosophy.md](docs/01-philosophy.md)를 먼저 읽는다. 모든 변경은 하나의 질문으로 판단한다. *무엇을 하는가, 무엇을 바꿀 수 있는가, 무엇을 보장하는가, 무엇이 실패할 수 있는가*에 답하기 쉬워지는가. 이 질문을 어렵게 만드는 기능은 아무리 영리해도 받지 않는다.

어떤 문법이 존재하는지는 [docs/11-grammar.md](docs/11-grammar.md)가 결정한다. 어떤 이름이 존재하는지는 [docs/12-stdlib.md](docs/12-stdlib.md)가 결정한다. 두 문서에 없는 문법이나 이름을 도입한다면 명세 수정이 먼저다.

## 개발 환경

최신 Rust 툴체인과 포터블 LLVM 17 설치가 필요하다. 설정 스크립트가 설치를 확인하고 백엔드가 기대하는 환경 변수를 출력한다.

```sh
git clone https://github.com/x1zzdev/Xz.git
cd Xz/xz-cli
scripts/setup-llvm.sh
cargo build
cargo test
```

`xz-cli/.cargo/config.toml`에는 이 저장소를 만든 장비의 LLVM 경로가 고정돼 있다. 다른 장비에서는 `scripts/setup-llvm.sh --emit`를 실행하거나 그 파일을 고친다. 자세한 내용은 [docs/13-codegen.md](docs/13-codegen.md)에 있다.

평소 작업 흐름은 다음과 같다.

```sh
cargo test
cargo run -- check ../examples/contracts.xz
cargo run -- run ../examples/hello.xz
```

CI는 `cargo test`를 실행한다. 관련 없는 파일을 통째로 재포맷하지 말고 주변 코드의 스타일을 따른다. 이 저장소는 rustfmt 기준으로 정규화된 상태가 아니다.

## 중요한 두 가지 원칙

1. **명세가 구현보다 먼저다.** 새 문법은 [docs/02-syntax.md](docs/02-syntax.md)와 관련 문서에 먼저 들어간다. 예제가 명세에 없는 기능을 필요로 하면 예제보다 명세를 먼저 고친다. 문법을 몰래 만들지 않는다.
2. **논리적 변경 하나당 커밋 하나.** 명세의 빈틈, 문서 수정, 예제 갱신은 관련 파일을 함께 건드려도 커밋 세 개다. 각 커밋은 그 자체로 완결돼야 한다. 링크가 살아 있고, 예제가 문서와 맞고, 테스트가 통과해야 한다.

## 커밋 메시지

파일 목록이 아니라 결정을 적는다.

```
Add handle semantics to close the Ptr aliasing hole
```

아래처럼 쓰지 않는다.

```
Update docs
```

## 풀 리퀘스트

풀 리퀘스트는 한 가지에 집중한다. 작업 중에 딴 문제를 발견하면 함께 묶지 말고 별도 이슈나 풀 리퀘스트로 낸다. 설명에는 무엇이 바뀌는지, 왜 바뀌는지, 이제 어느 문서가 그 동작의 근거인지 적는다.

명세가 계약이므로, 동작을 바꾸는 풀 리퀘스트는 새 동작을 설명하는 문서를 가리켜야 한다. 리뷰어는 자기 취향이 아니라 그 문서와 diff를 대조한다.

## 버그 신고와 기능 제안

이슈 템플릿을 사용한다. 버그 신고에는 최소한의 `.xz` 파일, 실행한 명령, 기대한 출력, 실제 출력이 있으면 가장 좋다. 컴파일러와 명세가 어긋난다면 어느 쪽이 틀렸다고 보는지 밝힌다.

## 행동 강령

참여에는 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)가 적용된다. 용납할 수 없는 행동은 ax1s@x1zz.com로 알린다.

## 라이선스

Xz는 MIT 또는 Apache-2.0 중 선택하는 듀얼 라이선스다. 기여를 제출하면 같은 조건으로 라이선스하는 데 동의하는 것이다. 정확한 문구는 [README_kr](README_kr.md#라이선스)에 있다.
